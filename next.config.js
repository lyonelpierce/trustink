/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  transpilePackages: ['@radix-ui/react-dropdown-menu', 'react-feather', 'tailwind-merge']
};

module.exports = nextConfig;
