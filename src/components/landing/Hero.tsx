import WidthWrapper from "../WidthWrapper";

const Hero = () => {
  return (
    <div>
      <WidthWrapper className="flex flex-col items-center justify-center pt-20 h-96">
        <h2 className="text-6xl font-semibold">
          A new era of smart agreements
        </h2>
      </WidthWrapper>
    </div>
  );
};

export default Hero;
